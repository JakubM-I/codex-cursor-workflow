# Cursor Materials

This directory is for Cursor-specific materials.

Use it for Cursor rules, implementation prompts, editor-native configuration, and skills or references intended primarily for Cursor.

Keep shared material in `.agents/` instead.
